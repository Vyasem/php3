<?xml version="1.0" encoding="UTF-8"?>

<project name="PHP-3" default="composer-install" basedir="D:/Projects/php3_lesson_1/origin">
    <target name="vars">
        <php function="date" returnProperty="build.time">
            <param value="d-m-Y-H-i-s" />
        </php>
        <property name="build.dir" value="D:/Projects/php3_lesson_1/${build.time}"/>
    </target>

    <target name="copy-files" depends="vars">
        <echo message="${build.dir}"/>
        <echo message="${project.basedir}"/>
        <copy todir="${build.dir}" overwrite="true">
            <fileset dir="${project.basedir}">
                <include name="**"/>
            </fileset>
        </copy>
    </target>

    <target name="make-config" depends="copy-files">
        <copy todir="${build.dir}/protected" file="${build.dir}/build/production/config.php" overwrite="true">
            <filterchain>
                <replacetokens begintoken="{{" endtoken="}}">
                    <token key="domain" value="test.loc" />
                </replacetokens>
            </filterchain>
        </copy>
    </target>

    <target name="composer-install" depends="copy-files">
        <exec command="D:/php7/php.exe composer install" dir="${build.dir}" passthru="true"/>
    </target>
</project>